commandlinefu_id: 13541
translator:
  weibo: ''
hide: true
command: |-
  awk '/text to grep/{print $1}' "log" | sort -n | uniq -c | sort -rn | head -n 100
summary: |-
  Parse logs for IP addresses and how many hits from each IP
