commandlinefu_id: 13224
translator:
  weibo: ''
hide: true
command: |-
  bashrc-reload() { builtin unalias -a; builtin unset -f $(builtin declare -F | sed 's/^.*declare[[:blank:]]\+-f[[:blank:]]\+//'); . ~/.bashrc; }
summary: |-
  Make changes in .bashrc immediately available
