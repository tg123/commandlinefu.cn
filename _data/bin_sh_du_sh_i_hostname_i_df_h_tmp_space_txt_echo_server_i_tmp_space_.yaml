commandlinefu_id: 1883
translator:
  weibo: gdqyn
command: |-
  #!/bin/sh #du.sh i=`hostname -i` df -h > /tmp/space.txt echo "server $i " >> /tmp/space.txt uuencode /tmp/space.txt space.txt | mail -s "HDD usage $i" email@email.com
summary: |-
  用Email发送磁盘使用情况
