commandlinefu_id: 3442
translator:
  weibo: titikaka723 
command: |-
  awk '{delta = $1 - avg; avg += delta / NR; mean2 += delta * ($1 - avg); } END { print sqrt(mean2 / NR); }'
summary: |-
  用AWK显示某一列数字的标准偏差 
