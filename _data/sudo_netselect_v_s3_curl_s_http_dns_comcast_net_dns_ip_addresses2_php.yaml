commandlinefu_id: 4678
translator:
  weibo: ''
hide: true
command: |-
  sudo netselect -v -s3 $(curl -s http://dns.comcast.net/dns-ip-addresses2.php | egrep -o '[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+' | sort | uniq)
summary: |-
  Find the fastest server to disable comcast's DNS hijacking
