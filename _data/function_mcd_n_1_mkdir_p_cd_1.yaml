commandlinefu_id: 2241
translator:
  weibo: ''
hide: true
command: |-
  function mcd() {   [ -n "$1" ] && mkdir -p "$@" && cd "$1";   }
summary: |-
  combine `mkdir foo && cd foo`  into a single function `mcd foo`
