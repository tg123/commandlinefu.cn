commandlinefu_id: 13219
translator:
  weibo: ''
hide: true
command: |-
  cat /proc/cpuinfo | grep BogoMIPS | uniq | sed 's/^.*://g' | awk '{print($1 / 4) }'
summary: |-
  calculate how much bogomips one cpu core has (assuming you have 4 cores).
