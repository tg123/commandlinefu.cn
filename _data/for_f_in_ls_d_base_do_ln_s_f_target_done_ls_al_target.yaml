commandlinefu_id: 1225
translator:
  weibo: ''
hide: true
command: |-
  for f in $(ls -d /base/*); do ln -s $f /target; done && ls -al /target
summary: |-
  Symlink all files from a base directory to a target directory
