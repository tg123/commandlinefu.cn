commandlinefu_id: 10233
translator:
  weibo: ''
hide: true
command: |-
  cmdfu(){ curl "http://www.commandlinefu.com/commands/matching/$(echo "$@" | sed 's/ /-/g')/$(echo -n $@ | base64)/plaintext" --silent | vim -R - }
summary: |-
  Search commandlinefu.com from the command line using the API
