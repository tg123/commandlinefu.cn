commandlinefu_id: 1073
translator:
  weibo: ''
hide: true
command: |-
  if [[ `:$(cal);echo $_` == `date +%d` ]]; then ROTATE_MONTHLY_TABLES_SCRIPT;fi
summary: |-
  Run a command if today is the last day of the month
