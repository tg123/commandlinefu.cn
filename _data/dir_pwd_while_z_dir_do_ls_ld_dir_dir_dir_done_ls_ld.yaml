commandlinefu_id: 4343
translator:
  weibo: ''
hide: true
command: |-
  dir=$(pwd); while [ ! -z "$dir" ]; do ls -ld "$dir"; dir=${dir%/*}; done; ls -ld /
summary: |-
  Show permissions of current directory and all directories upwards to /
