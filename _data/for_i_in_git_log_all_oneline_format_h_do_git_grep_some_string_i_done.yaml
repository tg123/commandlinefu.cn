commandlinefu_id: 2718
translator:
  weibo: ''
hide: true
command: |-
  for i in `git log --all --oneline --format=%h`; do git grep SOME_STRING $i; done
summary: |-
  search string in _all_ revisions
