commandlinefu_id: 1986
translator:
  weibo: ''
hide: true
command: |-
  for i in `ls  *xml`; do  sed -e 's,oldpassword,newpassword,g' $i > $i.2 && mv -f $i.2 $i  ; done
summary: |-
  Change password in list of xml files with for and sed
