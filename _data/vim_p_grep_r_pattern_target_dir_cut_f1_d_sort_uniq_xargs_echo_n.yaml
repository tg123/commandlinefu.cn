commandlinefu_id: 8968
translator:
  weibo: ''
hide: true
command: |-
  vim -p `grep -r PATTERN TARGET_DIR | cut -f1 -d: | sort | uniq | xargs echo -n`
summary: |-
  Grep across a directory and open matching files in vim (one tab per file)
